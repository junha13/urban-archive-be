<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="archive.backend.domain.auth.service.impl.AuthDAO">
    <select id="findByUserId" parameterType="String" resultType="archive.backend.domain.auth.service.UserVO">
        select
            user_number,
            login_id,
            login_password,
            created_at
        from tb_user
        where 1=1
        and login_id = #{userId}
    </select>

    <insert id="insertUser" parameterType="archive.backend.domain.auth.service.UserVO">
        insert into tb_user(login_id, login_password, student_id, name, nick_name, profile_image, email, lab, description, role, stuats, create_at)
        values (#{loginId}, #{loginPassword}, #{studentId}, #{name}, #{nickName}, #{profileImage}, #{email}, #{lab}, #{description}, 'ROLE_USER', 'ACTIVE', NOW())
    </insert>

    <select id="existByLoginId" parameterType="String" resultType="boolean">
        select count(*)
        from tb_user
        where login_id = #{loginId}
    </select>

    <select id="existByEmail" parameterType="String" resultType="boolean">
        select count(*)
        from tb_user
        where email = #{email}
    </select>

</mapper>